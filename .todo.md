Gameplay bugs:
- [ ] Should be able to undo/confirm even when confirm would lose the game.

Features:
- [ ] _Lost Visual_ mini-expansion.

Questions:
- [ ] does gameoptions have to be 100-indexed / 1-indexed? or can it be string keys?
- [ ] Climb Window ability — "your actions end after you use this ability" how does this interact with skates, etc?

Todo:
- [ ] audit map generation; there's a check for "couldn't generate a valid map" but we should definitely be able to generate a valid map, right?
- [ ] `$floor_tiles[$shaft]['v'] = TRUE;` ?
- [ ] use `init` instead of deprecated `initStat`?
- [ ] more enum/consolidate string constants & int constants
  - [ ] deck keys 'characters' 'tools' 'loot' 'events'
- [ ] update from deprecated BgaUserException to UserException
  - [ ] redo the ugly red error messages
- [ ] add type(s) for $machinestate
- [ ] clean up GameOption
- [ ] think about migration for PlayerChoiceArg vs UndoAllowed; probably just copy the value if the destination doesn't exist. and move PlayerChoiceArg to the end to avoid this happening again. I think the issue was that someone added it out of order.
- [ ] Probably the alpha table gate doesn't need to exist anymore
- [ ] look into wrapper functions to hide enum->value from main logic.
